## Prefect's Open Source Server and UI

Spinning up Prefect Core's backend, API and UI locally can be achieved with a single command:

```
prefect server up
```

### Development

#### Backend / API

#### UI
