test:
    uv run pytest

# Generate API reference documentation for prefect-dbt
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-dbt \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_dbt \
        --output-dir docs/integrations/prefect-dbt/api-ref \
        --anchor-name "prefect-dbt-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
