test:
    uv run pytest

# Generate API reference documentation for prefect-sqlalchemy
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-sqlalchemy \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_sqlalchemy \
        --output-dir docs/integrations/prefect-sqlalchemy/api-ref \
        --anchor-name "prefect-sqlalchemy-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
