# Generate API reference documentation for prefect-openlineage
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-openlineage \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_openlineage \
        --output-dir docs/integrations/prefect-openlineage/api-ref \
        --anchor-name "prefect-openlineage-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
