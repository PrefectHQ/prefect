test:
    uv run pytest

# Generate API reference documentation for prefect-slack
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-slack \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_slack \
        --output-dir docs/integrations/prefect-slack/api-ref \
        --anchor-name "prefect-slack-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
