test:
    uv run pytest

# Generate API reference documentation for prefect-snowflake
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-snowflake \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_snowflake \
        --output-dir docs/integrations/prefect-snowflake/api-ref \
        --anchor-name "prefect-snowflake-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
