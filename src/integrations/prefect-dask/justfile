test:
    uv run pytest

# Generate API reference documentation for prefect-dask
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-dask \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_dask \
        --output-dir docs/integrations/prefect-dask/api-ref \
        --anchor-name "prefect-dask-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
