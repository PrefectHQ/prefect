test:
    uv run pytest

# Generate API reference documentation for prefect-github
api-ref:
    #!/usr/bin/env bash
    REPO_ROOT="$(git rev-parse --show-toplevel)"
    cd "$REPO_ROOT"
    uvx --with-editable ./src/integrations/prefect-github \
        --python 3.12 \
        --isolated \
        mdxify \
        --all \
        --root-module prefect_github \
        --output-dir docs/integrations/prefect-github/api-ref \
        --anchor-name "prefect-github-sdk-reference" \
        --repo-url https://github.com/PrefectHQ/prefect \
        --include-inheritance
