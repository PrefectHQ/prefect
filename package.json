{
    "name": "prefect",
    "description": "Prefect is a brand-new workflow manager.",
    "version": "0.3.0",
    "dependencies": {},
    "scripts": {
        "docs:dev": "cd docs && python generate_docs.py && vuepress dev .",
        "docs:build": "cd docs && python generate_docs.py && vuepress build . -d deploy/dist",
        "docs:deploy": "gcloud app deploy docs/deploy --quiet --project=prefect-docs"
    },
    "repository": {
        "type": "git",
        "url": "git+https://gitlab.com/prefect/prefect.git"
    },
    "devDependencies": {
        "vuepress": "^0.10.1"
    }
}