"""
Fixtures for tests/results/ module.

This module is excluded from the clear_db auto-mark, meaning the database is not
cleared before each test. Block type slug conflicts are prevented by other test
modules using unique slugs via dynamically-named Block classes.
"""
