version: 2
jobs:
  run_tests:
    docker:
      - image: python:3.4

    steps:
      - checkout
      - run: |
          pip install ".[dev]"
          pytest -v

workflows:
    version: 2
    test:
      jobs:
        - run_tests
